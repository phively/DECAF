[info]
name = fuzzy match company ; appended to output file
description = Standardize and compare elementwise two lists of company names

[parameters]
match_threshold = 80 ; match = True if score >= threshold

[control]
functions = FuzzyMatch.fuzzy_match_pairwise, FuzzyMatch.threshold_score
cleaning = cleaning/clean_company_name.ini
